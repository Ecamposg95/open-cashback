# Open Cashback Contracts

Contrato MVP para anchoring de hashes de batches del ledger interno.

`OpenCashbackLedger` no maneja fondos ni tokens. Solo registra `batchHash`, evita duplicados y emite eventos para auditoria. La tokenizacion queda en roadmap.
